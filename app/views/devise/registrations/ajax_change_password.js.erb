
var message = "<%= @message %>" ;

if(message == "success"){
	$('.modal').modal("hide") ;
	$("form").trigger("reset");var form_enabled = false;
    toastr.success('Updated Password', 'Successfully');
}else{
    toastr.error('Update Password failed. Please enter correct current passowrd', 'Sorry');
}